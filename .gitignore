# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/
*.sublime-*

# PHP / runtime (Composer packages — not our bundled Leaflet libs under vendor/)
composer.lock
.env
.env.*
!.env.example

# Local database dumps; keep seeded offline map for Derna pilot
data/tiles/*
!data/tiles/.gitkeep
!data/tiles/libya.mbtiles
!data/tiles/libya-sat.mbtiles
!data/tiles/libya-labels-transport.mbtiles
!data/tiles/libya-labels-places.mbtiles

# Logs & temp
*.log
tmp/
cache/

# Windows batch local overrides (if you add secrets there)
run-server.local.bat
